import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary:{
            main: "#0052cc",
            light: "lightblue",
        },
        secondary: {
          main: '#edf2ff',
          light: "lightorange",
        },
    }
})